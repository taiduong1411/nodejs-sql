<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inbox</title>
  <link rel="stylesheet" href="/styles/main.css">
  <style>
    body {
      font-family: 'Quicksand', sans-serif;
      background-color: #f5f5f5; 
      color: #444; 
    }
    
    .container {
      max-width: 700px;
      margin: 30px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 4px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1); 
    }

    h1 {
      color: #007bff; 
      font-weight: 400;
      margin: 30px 0 15px;
      line-height: 1.2;
    }

    .email-list {
      list-style-type: none;
      padding: 0;
    }

    .email-list li {
      border: 1px solid #ddd;
      margin-bottom: 15px;
      padding: 15px;
      border-radius: 10px;
      background-color: #f9f9f9;
    }

    .email-list li a {
      text-decoration: none;
      color: #333;
      font-weight: bold;
    }

    .email-list li a:hover {
      text-decoration: underline;
    }

    p {
      margin-top: 20px;
    }

    .compose-link,.outbox-link,.sign-out-link {
      display: inline-block;
      margin-right: 10px;
      text-decoration: none;
      padding: 12px 18px;
      border-radius: 5px;
      color: #fff;
      background-color: #007BFF;
      transition: background-color 0.3s ease;
    }

    .compose-link:hover,.outbox-link:hover,.sign-out-link:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Inbox</h1>

    <% if (emails && emails.length > 0) { %>
      <ul class="email-list">
        <% emails.forEach(email => { %>
          <li>
            <a href="/email/<%= email.id %>">
              <strong><%= email.subject %></strong>
            </a>
            <p>Sent by <%= email.sender_name %> on <%= email.sent_at %></p>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>No emails found in the inbox.</p>
    <% } %>

    <p>
      <a href="/compose" class="compose-link">Compose a new email</a>
      <a href="/outbox" class="outbox-link">View Outbox</a>
      <a href="/" class="sign-out-link">Sign Out</a>
    </p>
  </div>
</body>
</html>

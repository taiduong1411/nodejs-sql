<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Outbox</title>
  <link rel="stylesheet" href="/styles/main.css">
  <style>
    body {
      font-family: 'Quicksand', sans-serif;
      background-color: #f5f5f5; 
      color: #444; 
    }
    
    .container {
      max-width: 700px;
      margin: 30px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 4px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1); 
    }

    h1 {
      color: #007bff; 
      font-weight: 400;
      margin: 30px 0 15px;
      line-height: 1.2;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      border: 1px solid #ddd;
      margin-bottom: 15px;
      padding: 15px;
      border-radius: 10px;
      background-color: #f9f9f9;
    }

    li strong {
      display: block;
      font-size: 1.2em;
      margin-bottom: 10px;
      color: #333;
    }

    li p {
      margin: 5px 0;
    }

    li a {
      text-decoration: none;
      color: #007BFF;
      font-weight: bold;
      display: block;
      margin-top: 10px;
      transition: color 0.3s ease;
    }

    li a:hover {
      color: #0056b3;
    }

    p {
      margin-top: 20px;
    }

    a {
      display: inline-block;
      text-decoration: none;
      padding: 12px 18px;
      border-radius: 5px;
      color: #fff;
      background-color: #007BFF;
      transition: background-color 0.3s ease;
    }

    a:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Outbox</h1>

    <% if (emails.length > 0) { %>
      <ul>
        <% emails.forEach(email => { %>
          <li>
            <strong><%= email.subject %></strong>
            <p>Recipient: <%= email.recipient_id %></p>
            <p>Sent: <%= email.sent_at %></p>
            <a href="/email/<%= email.id %>">View Details</a>
          </li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>Your outbox is empty.</p>
    <% } %>

    <a href="/inbox">Go to Inbox</a>
  </div>
</body>
</html>
